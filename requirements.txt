# Core dependencies (Windows 10/11, Python 3.11+)
pymupdf>=1.24.0
Pillow>=10.0.0
pytesseract>=0.3.10
pyttsx3>=2.90

# Optional: better Windows voice enumeration hints (pyttsx3 uses SAPI5 natively)
# No cloud APIs — fully local.
